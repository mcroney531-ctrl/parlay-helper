// Named thresholds for deterministic organizing rules. Keep these as the
// single source of truth — never inline a magic number for a rule threshold.

export const CONCENTRATION_MIN_LEGS = 4;
export const CONCENTRATION_SHARE_THRESHOLD = 0.5;
export const CORRELATION_MIN_SHARED_LEGS = 2;
