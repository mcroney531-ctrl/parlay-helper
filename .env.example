# Optional. Without it, /api/odds and /api/events return an explicit
# not_configured status per event/league instead of failing — the core
# capture -> build -> finalize loop never depends on this being set.
# Get a key at https://the-odds-api.com/
ODDS_API_KEY=
